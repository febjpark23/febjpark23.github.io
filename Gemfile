# Adding this line so that we can ask ruby to install the minima theme for the 
# site. You may install as many themes as you want. To use a theme, specify it 
# in the `_config.yml` file.
gem "minima"